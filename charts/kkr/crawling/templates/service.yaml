apiVersion: v1
kind: Service
metadata:
  name: {{ include "crawling.fullname" . }}
  labels:
    {{- include "crawling.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "crawling.selectorLabels" . | nindent 4 }}
