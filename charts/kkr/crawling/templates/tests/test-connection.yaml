apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "crawling.fullname" . }}-test-connection"
  labels:
    {{- include "crawling.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "crawling.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
