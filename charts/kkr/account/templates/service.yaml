apiVersion: v1
kind: Service
metadata:
  name: {{ include "kkr-account.fullname" . }}
  labels:
    {{- include "kkr-account.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kkr-account.selectorLabels" . | nindent 4 }}
